# Copy to repo root .env or export before run (all optional)

PYTHONPATH=.
PULL_FROM_GCP=false

# GCP (model: gs://ceres-ai-bucket/models/ml_model.pkl)
CERES_GCS_BUCKET=ceres-ai-bucket
CERES_GCS_MODEL_BLOB=models/ml_model.pkl

# API
CERES_API_HOST=127.0.0.1
CERES_API_PORT=8000
