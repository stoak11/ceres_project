"""Streamlit UI — to be wired in a follow-up step."""
from __future__ import annotations

import streamlit as st

st.set_page_config(page_title="Ceres Predict", layout="centered")
st.title("Ceres Predict")
st.info("Streamlit UI coming next — start the API with `./scripts/run_predict_api.sh`")
